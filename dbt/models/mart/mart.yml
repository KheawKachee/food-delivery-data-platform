version: 2

models:
  - name: delivery_time
    columns:
      - name: order_id
        tests: [not_null, unique]

  - name: hourly_total_spends
    columns:
      - name: hourly
        tests: [not_null, unique]

  - name: avg_rider_rating
    columns:
      - name: hourly
        tests: [not_null, unique]

